{
  "scripts": {
    "setup": "npm install && bundler install",
    "build": "npx babel src --out-dir assets/2017/js && bundle exec jekyll build",
    "start": "bundle exec jekyll serve"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  }
}
